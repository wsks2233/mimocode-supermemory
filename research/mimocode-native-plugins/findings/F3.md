# F3: MiMoCode 与 MiMo Desktop 现有扩展面（MCP / skills / hooks / config / 会话）及距「原生插件」的缺口

## Findings

### [1] MiMoCode 明确是 OpenCode 分支，官方自述「保留全部 OpenCode 核心能力（multi-provider, TUI, LSP, MCP, plugins）」，再叠加记忆/上下文/子代理/Compose/进化能力
- quote: "MiMoCode is built as a fork of OpenCode. It keeps all core OpenCode capabilities (multiple providers, TUI, LSP, MCP, plugins) and adds persistent memory, intelligent context management, subagent orchestration, goal-driven autonomous loops, compose workflows, and self-improvement via dream/distill."
- url: https://raw.githubusercontent.com/XiaomiMiMo/MiMo-Code/main/README.md
- source_type: primary
- published: unknown (repo HEAD as of 2026-09-19 fetch)
- confidence: high

### [2] MiMoCode 配置 schema 保留 OpenCode 式 `plugin` 键：数组元素为 npm 包名字符串或 `[string, options]` 元组；CLI 亦有 `mimo plugin`（别名 `plug`）子命令
- quote: "\"plugin\": { \"type\": \"array\", \"items\": { \"anyOf\": [ { \"type\": \"string\" }, { \"type\": \"array\", \"prefixItems\": [ { \"type\": \"string\" }, { \"type\": \"object\", ... } ] } ] } }"
- url: https://mimo.xiaomi.com/mimocode/config.json
- source_type: primary
- published: schema live as of 2026-09-19
- confidence: high
- note: 同一 schema 中亦含 `skills.paths[]` / `skills.urls[]`（远程 skill 索引）、`mcp`、`command`；产品文档 `commands.md` 列出 `mimo plugin (plug) | Manage plugins`。

### [3] 官方已发布插件 SDK 包 `@mimo-ai/plugin@0.1.14`（MIT，Xiaomi MiMo Team，repo `XiaomiMiMo/MiMo-Code` 的 `packages/plugin`），导出 `.` / `./tool` / `./tui` 三个入口
- quote: "\"name\": \"@mimo-ai/plugin\", \"version\": \"0.1.14\", \"description\": \"Plugin SDK for extending MiMoCode\" ... \"exports\": { \".\": { ... }, \"./tool\": { ... }, \"./tui\": { ... } }"
- url: file://C:/Users/<local>/.config/mimocode/node_modules/@mimo-ai/plugin/package.json（本机安装副本；与 package-lock 中 `registry.npmjs.org/@mimo-ai/plugin/-/plugin-0.1.14.tgz` 一致）
- source_type: primary
- published: 0.1.14（lock 解析时间戳见本机 package-lock）
- confidence: high

### [4] `@mimo-ai/plugin` 的服务端插件模型与 OpenCode 同构：`Plugin = (input, options) => Promise<Hooks>`，`PluginInput` 提供 `client`（SDK）、`project`、`directory`、`worktree`、`$`（Bun shell）以及实验性 `experimental_workspace.register` workspace adaptor
- quote: "export type Plugin = (input: PluginInput, options?: PluginOptions) => Promise<Hooks>; ... experimental_workspace: { register(type: string, adaptor: WorkspaceAdaptor): void; };"
- url: file://C:/Users/<local>/.config/mimocode/node_modules/@mimo-ai/plugin/dist/index.d.ts
- source_type: primary
- published: package 0.1.14
- confidence: high

### [5] OpenCode 父项目插件面文档化完整：本地目录 `.opencode/plugins/` + `~/.config/opencode/plugins/` 自动加载，或 config `plugin: ["npm-pkg", ...]` 由 Bun 自动安装到 `~/.cache/opencode/node_modules/`；钩子覆盖 tool/session/message/permission/shell/TUI 等约 20+ 事件
- quote: "Plugins allow you to extend OpenCode by hooking into various events... Place JavaScript or TypeScript files in the plugin directory. `.opencode/plugins/` - Project-level plugins... `{ \"plugin\": [\"opencode-helicone-session\", \"opencode-wakatime\", \"@my-org/custom-plugin\"] }`"
- url: https://opencode.ai/docs/plugins/
- source_type: primary
- published: page footer "Last updated: Sep 18, 2026"
- confidence: high

### [6] MiMoCode 自身文档化了并行的「文件级自扩展」面（evolve）：工具/钩子/技能/工作流/TUI 插件分别落在 `.mimocode/tools|hooks|skills|workflows|tui`，除 TUI 需重启外热加载到下一回合；扩展类型从 `@mimo-ai/plugin` 导入
- quote: "To add tools, hooks, or skills, use the `evolve` skill — it covers writing `.mimocode/tools/*.ts`, `.mimocode/hooks/*.ts`, and `.mimocode/skills/*/SKILL.md`, all hot-reloaded on the next turn."
- url: file://C:/Users/<local>/.local/share/mimocode/builtin_skills/desktop-5198ff5/skills/mimocode-docs/reference/guide.md（同源：…/skills/evolve/SKILL.md）
- source_type: primary
- published: desktop-5198ff5 skill pack (as of 2026-09-19)
- confidence: high

### [7] 官方中文文档把自定义工具写成一等公民：TS/JS 定义位于项目 `.mimocode/tools/` 与全局 `~/.config/mimocode/tools/`；文件名即工具 id；同名覆盖内置工具；多导出命名为 `<filename>_<export>`；可用 Zod/`tool.schema` 校验；文档侧栏「自定义」列出 Themes/Commands/MCP/Skills/Agents/Formatters/LSP/Custom tools/Rules，**未列独立 Plugins 专页**
- quote: "工具可以在以下位置定义：本地定义：将工具文件放在项目的 `.mimocode/tools/` 目录中。全局定义：将工具文件放在 `~/.config/mimocode/tools/` 中。 ... 如果自定义工具使用了与内置工具相同的名称，则优先使用自定义工具。"
- url: https://mimo.xiaomi.com/zh/mimocode/custom-tools
- source_type: primary
- published: docs site as of 2026-09-19
- confidence: high

### [8] MCP 是当前最完整的「外部能力」入口：`mcp` 配置支持 local（command/env/cwd/timeout 默认 5000ms）与 remote（url/headers/oauth，RFC 7591 动态客户端注册，token 存 `~/.local/share/mimocode/mcp-auth.json`）；组织可经 `.well-known/mimocode` 下发默认服务器；工具可用 `tools` 键 + glob 与 per-agent 覆盖开关
- quote: "你可以通过 *Model Context Protocol*（MCP）为 MiMo Code 添加外部工具。MiMo Code 同时支持本地和远程服务器。 ... 组织可以通过其 `.well-known/mimocode` 端点提供默认的 MCP 服务器。 ... MCP 服务器工具在注册时以服务器名称作为前缀"
- url: https://mimo.xiaomi.com/zh/mimocode/mcp-servers
- source_type: primary
- published: docs site as of 2026-09-19
- confidence: high
- note: CLI 侧有 `mimo mcp add|list|auth|logout|debug`（https://mimo.xiaomi.com/zh/mimocode/cli-subcommands）。

### [9] Skills 面在配置层已支持远程分发：`skills.paths[]`（额外本地 skill 目录）与 `skills.urls[]`（例如 `https://example.com/.well-known/skills/` 远程 skill 索引）；桌面端另有 Plugins 页聚合 marketplace + 用户技能 + 系统技能，并读取 `locales/<locale>.json` 的 `displayName`/`brief`
- quote: "\"urls\": { \"description\": \"URLs to fetch skills from (e.g., https://example.com/.well-known/skills/)\", \"type\": \"array\", \"items\": { \"type\": \"string\" } }"
- url: https://mimo.xiaomi.com/mimocode/config.json
- source_type: primary
- published: schema live as of 2026-09-19
- confidence: high
- note: Desktop 约定见 `mimo-skill-authoring`：目录名=稳定 ID，须与 frontmatter `name` 一致；仅字母数字连字符。

### [10] MiMo Desktop 对扩展路径做了品牌隔离：只在 MiMoCode 根（`~/.config/mimocode/skills`、`<project>/.mimocode/skills/`）安装/导入/扫描；`~/.claude|~/.codex|~/.agents|~/.opencode/skills` **不被扫描**；MCP 变更仅对下一会话生效、不热加载到当前会话
- quote: "Desktop **installs/imports only** under MiMoCode roots (`~/.config/mimocode/skills`, project `<project>/.mimocode/skills/`). Skills under brand roots (`~/.claude/skills`, `~/.codex/skills`, `~/.agents/skills`, `~/.opencode/skills`) are **not scanned or loaded** by MiMo Desktop. ... MCP: Settings -> MCP configures Model Context Protocol servers over stdio or HTTP. Additions and removals apply to the next new conversation and do not hot-reload into the current one."
- url: file://C:/Users/<local>/AppData/Roaming/Xiaomi%20MiMo/engine-config/skills/mimo-desktop-guide/SKILL.md
- source_type: primary
- published: desktop skill pack as of 2026-09-19
- confidence: high

### [11] 关键权限缺口：`permission.ask` 钩子已在 Hooks 接口中定义但「尚未接入权限系统」；且官方明确「权限不可被自定义 tools/hooks 修改」——扩展面无法覆盖安全策略层
- quote: "permission.ask | Auto-allow/deny permission requests (not yet wired) ... Permissions cannot be modified by custom tools/hooks — they are the one system the self-extension surface can't override."
- url: file://C:/Users/<local>/.local/share/mimocode/builtin_skills/desktop-5198ff5/skills/evolve/reference/hook-api.md；file://C:/Users/<local>/.local/share/mimocode/builtin_skills/desktop-5198ff5/skills/mimocode-docs/reference/permissions.md
- source_type: primary
- published: desktop-5198ff5 skill pack
- confidence: high

### [12] 会话/生命周期钩子已覆盖可分析与可拦截场景：`session.pre/post`（可 cancel；post 携带完整 trajectory）、`session.userQuery.pre/post`（逐步 cancel/inspect）、`actor.preStop/postStop`（子代理交付门禁，`continue=true` 强制再跑）、`experimental.chat.system.transform` / `messages.transform`、`chat.params` / `chat.headers`、`experimental.session.compacting`
- quote: "session.pre / session.post | Session runLoop lifecycle; `post` receives the full trajectory ... actor.preStop / actor.postStop | Gate subagent delivery; `continue=true` forces another turn"
- url: file://C:/Users/<local>/.local/share/mimocode/builtin_skills/desktop-5198ff5/skills/evolve/SKILL.md；同目录 reference/hook-api.md
- source_type: primary
- published: desktop-5198ff5 skill pack
- confidence: high

### [13] TUI 插件（CLI 界面）已有插槽/命令/路由/KV/事件 API，但渲染绑定 opentui+Solid.js 终端元素（非 HTML/CSS），且仅在启动时扫描、需 restart；`tui.json` 的 `plugin` 数组可声明 file URL 或 npm spec
- quote: "Files are auto-discovered from two directories at TUI startup: `~/.config/mimocode/tui/*.{ts,tsx,js}` — global; `<project>/.mimocode/tui/*.{ts,tsx,js}` — project-local. ... Components render to the **terminal** via opentui + Solid.js. HTML elements (`<div>`, `<span style=\"css\">`, CSS properties) do not exist."
- url: file://C:/Users/<local>/.local/share/mimocode/builtin_skills/desktop-5198ff5/skills/evolve/reference/tui-api.md
- source_type: primary
- published: desktop-5198ff5 skill pack
- confidence: high

### [14] Desktop 侧公开扩展 UI 极窄：仅 `get_desktop_settings` / `set_desktop_setting` 且键位固定为 theme / ui_font_size / code_font_size / diff_style / language；主题色与字体「产品定义且不开放自定义」；Browser Use / Computer Use 以「安装并启用插件 → 统一 MCP + skill」方式接入，而非独立 `browser_*` 工具
- quote: "Allowed keys: theme / ui_font_size ... / code_font_size ... / diff_style / language ... If the user asks to customize theme colors, foreground/background colors, or font families, explain that the product defines them and does not expose custom settings. ... After the Browser Use plugin is installed and enabled, the agent controls the in-app browser through the unified Browser Use MCP and skill. There are no separate `browser_*` tools."
- url: file://C:/Users/<local>/AppData/Roaming/Xiaomi%20MiMo/engine-config/skills/mimo-desktop-guide/SKILL.md
- source_type: primary
- published: desktop skill pack as of 2026-09-19
- confidence: high

### [15] 距「原生插件」的结构性缺口（综合一手材料）：能力以 **碎片化并行面** 存在——OpenCode 遗产 `plugin`/`mimo plugin`、文件级 evolve（tools/hooks/skills/workflows/tui）、MCP 远程工具、skills 远程索引与 Desktop Plugins 页——但缺少统一「原生插件」包格式：没有把 MCP server + skill + hook + custom tool + Desktop UI + 权限策略 + 版本/签名/市场分发打成单一可安装单元的公开规范；公开中文文档甚至没有 Plugins 专页；`permission.ask` 未接线；Desktop 不扫跨品牌 skill 根且 MCP 不热加载；TUI 插件与 Electron Desktop 插件模型割裂
- quote: "MiMoCode bundles the following builtin skills... **Overriding a builtin skill:** Create a skill with the same name in your project (`.mimocode/skills/<name>/SKILL.md`) ... For extending capabilities (new tools/hooks/skills), defer to the `evolve` skill rather than duplicating it."
- url: https://raw.githubusercontent.com/XiaomiMiMo/MiMo-Code/main/README.md；https://mimo.xiaomi.com/zh/mimocode/custom-tools；evolve + mimo-desktop-guide skill packs
- source_type: primary
- published: synthesized from primary docs as of 2026-09-19
- confidence: medium
- note: 这是 gap 评估而非单一文档原文；支撑材料均为产品一手文档/skill/包与 schema。

## Dead ends
- Web search 工具在本环境不可用（「Web Search plugin」未启用），无法按「up to 8 web searches」执行检索；改为直接 WebFetch 一手 URL + 读取本机已安装的官方 skill/SDK/schema。
- `https://github.com/XiaomiMiMo/MiMo-Code` 与 `https://github.com/supermemoryai/opencode-supermemory` 直接 fetch 超时/传输错误；MiMo-Code 内容改由 `raw.githubusercontent.com/.../README.md` 与 `mimo.xiaomi.com` 官方文档补齐。
- `https://opencode.ai/docs/custom-tools/` 与 `docs/ecosystem/` 超时；OpenCode 插件/MCP/Skills 文档已成功 fetch，足以支撑父项目对照。
- `https://www.npmjs.com/package/@mimo-ai/plugin` 传输错误；以本机 `node_modules/@mimo-ai/plugin`（package.json + dist/*.d.ts + package-lock 指向 npmjs 0.1.14）作为一手证据。
- `https://mimo.xiaomi.com/zh/mimocode/skills` 传输错误；skills 配置语义改由 config schema、README、Desktop skill-authoring 文档覆盖。
- `supermemoryai/opencode-supermemory` 本角度未能读取原文；不影响 F3 的 MiMoCode 扩展面结论，留给横向对比任务。

## Suggested follow-ups
- 深挖 `mimo plugin` 子命令实际行为与 `~/.cache/mimocode` 插件安装路径：是否完整继承 OpenCode Bun 自动安装 + 生态插件，还是 schema 残留而桌面/CLI 路径未产品化。
- 对照 OpenCode `Plugin` 事件全集与 `@mimo-ai/plugin@0.1.14` Hooks 类型差集，列出 MiMoCode 新增/缺失/未接线钩子（尤其 `permission.ask`、`chat.headers`、workspace adaptor）。
- 检查 Desktop 引擎是否暴露与 TUI `slots/route/command` 等价的 Electron 侧扩展 API（或仅 skill+MCP+appearance 三件套）；若有 SDK 事件 `pluginName` / `pluginNames` / `mcp.tools.changed`，确认其在 Desktop 会话中的可订阅性。
